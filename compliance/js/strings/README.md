# Strings Suite

This suite will hold real upstream JavaScript string compliance tests that are translated into committed C fixtures under `compliance/generated/strings/`.

Prefer small, semantically focused cases first: normalization, casing, concatenation, UTF-16 length/index behavior, and well-formedness.
