// Repo-authored rewrite-backed fixture for String.prototype.split over a
// fixed literal `/u` UTF-16 sequence with no captures.
//
// The sequence helper must split only on code-point-boundary-aligned matches.
