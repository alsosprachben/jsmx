// Repo-authored parity fixture for the optional regex backend.
//
// This case proves that parsed JSON-backed strings and equivalent native
// strings behave the same through the jsval -> regex-search bridge when the
// PCRE2 backend is enabled.
