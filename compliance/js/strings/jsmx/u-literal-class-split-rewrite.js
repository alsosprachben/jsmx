// Repo-authored rewrite-backed fixture for String.prototype.split over a
// single no-capture `/u` character class with explicit literal members.
//
// The class helper must split only on isolated class-member matches and must
// never split inside a surrogate pair.
