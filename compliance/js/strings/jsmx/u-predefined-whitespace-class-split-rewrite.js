// Repo-authored rewrite-backed fixture for single predefined `/u`
// whitespace classes. The translator lowers `\\s` directly to the dedicated
// jsval split helper so JS WhiteSpace + LineTerminator behavior stays explicit.
