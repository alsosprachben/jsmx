// Repo-authored WHATWG Fetch stub fixture.
// fetch() constructs a Request and returns a rejected Promise because the
// jsmx network transport is deferred to a later slice.
fetch("https://example.com");
