# String Fixtures

This suite contains generated C fixtures translated from real JavaScript string compliance tests.

Prefer `jsstr` when the source test only exercises string semantics. Use `jsval` only when the string case crosses a JSON/value boundary that matters to generated C.
